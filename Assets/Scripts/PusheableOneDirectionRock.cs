using UnityEngine;

public class PusheableOneDirectionRock : MonoBehaviour, IMovable
{

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
    }


    public void Execute(Vector3 direction)
    {

    }
}
